import React from 'react';

const Header = () => {
    return (
        <header>
            <div className={`container mx-auto`}>
                test
            </div>
        </header>
    );
};

export default Header;